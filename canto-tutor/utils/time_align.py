"""Time alignment module stubs."""

from __future__ import annotations

from pathlib import Path


def align_chars_to_timestamps(lyrics_lines: list[str], timestamps_path: Path) -> list[tuple[float, float]]:
    """Align lyric characters with timestamp intervals (stub)."""
    _ = lyrics_lines
    _ = timestamps_path
    return []
